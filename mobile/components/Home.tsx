import React from 'react'
import {Button, View} from 'react-native'

const Home = () => {
    return (
        <View>
            <Button title='Button'/>
        </View>
    );
};

export default Home;

